<p id="notice"><%= notice %></p>
<h1 class="text-center">  <div class="otopright">
    <span class="icons" id="tcard"><i class="fa fa-columns"></i></span>
    <span class="icons" id="ttable"><i class="fa fa-th-list"></i></span>
  </div>
  Товари

</h1>

<div class="tcard active">
  <div class="row p-3">
    <%=render @products %>
  </div>
</div>

<div class="ttable">
  <table class="table table-striped table-active">
    <thead class="thead-dark">
      <tr>
        <th>Фото</th>
        <th>Назва</th>
        <th>Опис</th>
        <th>Ціна</th>
          <th colspan="10"></th>
      </tr>
    </thead>

    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td>
            <% if product.images.present?%>
              <%= image_tag product.images.first,width: 250 %>
            <%end%>
          </td>
          <td>
          <%= link_to product do %>
            <%= product.name %>
          <%end%>
        </td>
        <td><%= product.desc %></td>
        <td><%= product.price %> грн.</td>
        <% if user_signed_in? && current_user.id ==1 %>
          <td><%= link_to 'Edit', edit_product_path(product) %>
            <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%else%>
          <td>
            <%= link_to "Замовити", new_product_order_path(product.id), class:'uploadButton ' %>
          </td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
  </table>
</div>

<script>
  $("#tcard").on("click", function(){
    $(".ttable").removeClass("active");
    $(".tcard").addClass("active");
  });
  $("#ttable").on("click", function(){
    $(".tcard").removeClass("active");
    $(".ttable").addClass("active");
  });
</script>
